#!/usr/bin/expect

set timeout 30
spawn ssh [lindex $argv]@[lindex $argv 1]
expect {
        "(yes/no)"
        {send "yes\n";exp_continue}
        "password:"
        {send "[lindex $argv 2]\n"}
}
interact
